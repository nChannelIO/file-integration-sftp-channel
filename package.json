{
  "name": "file-integration-sftp-channel",
  "version": "1.0.0",
  "description": "File integration via SFTP",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@nchannel/endpoint-sdk": "^1.0.1"
  }
}
